#include<stdio.h>
int main (){
    int a; //good question for dsa thankyou mysirg.
    a=20>10>5; //  20 is geter than 10 is not greater than 5 is called code run to true and false 1 and 0=false and 1= true
    printf("%d",a);
    return 0;

}